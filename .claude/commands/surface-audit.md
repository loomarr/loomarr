---
description: Find capabilities that exist in the API but have no user-facing path
---

Read `.agents/workflows/surface-audit.md` and follow it exactly. Pass `$ARGUMENTS` as the workflow's
argument.
