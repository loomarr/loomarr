# Deterministic installs for the committed-artifact discipline (fe-tokens/orval
# diffs must be reproducible in CI). Hoist nothing implicitly the app relies on.
auto-install-peers=true
strict-peer-dependencies=false
resolution-mode=highest
