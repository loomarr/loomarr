{
  "name": "@loomarr/fixtures",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Deterministic \"test card\" fixtures for Storybook stories + tests (web + future mobile).",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@loomarr/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^3.2.7"
  }
}
