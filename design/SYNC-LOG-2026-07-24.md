# Design-project sync log (verbatim copy)

Fetched 2026-07-24 from Claude Design project `dc543738-1b55-41c0-a295-a693b65aef05`,
file `github.md`. Copied here because it is the evidence that the v2 prototypes were built
*from* this repo rather than independently — see
`docs/engineering/v2-mock-delta-2026-07-24.md` §2a, where every claim below is verified
against HEAD `55fc691`.

Do not edit; this is a transcript.

---

repo: loomarr/loomarr
branch: main

## Last sync

date: 2026-07-24T21:51:03Z

### Updated in this project

- Rebuilt channel detail as the real 5-tab surface from `channels/$id.tsx`: Channel info, Programming, Filler, Advanced, Danger zone — with inline name/number editing in the header.
- Programming folds Refine-with-AI (diff → apply), "What plays" (lineup editor + audience ceiling + era), "How it's ordered" (ordering + no-repeat windows), "When it changes" (time rules + presets) and the shared cycle preview, per `-channel-programming.tsx`.
- Filler tab mirrors the draft/apply sandbox (`channel-filler.tsx`): criteria chips, always/never include, live break preview, Unsaved dot, Apply/Discard.
- Advanced renders relaxation-ladder sentences + the Tunarr link (`-channel-advanced.tsx`); Danger zone is pause/resume + two-step delete with the "Also remove it from Tunarr" purge checkbox.
- Added a per-channel ⋮ row menu on the guide (Edit / Pause-Resume / Delete) from `channel-row-menu.tsx`.
- Guide gained a calendar date picker, start-time control, and −/+ overall zoom.
- Every wizard step is now required (product decision — diverges from `steps.ts`, which marks Users `optional: true`).

- Rebuilt the first-run wizard to the real 5-step shape (Admin → Connections → Library → Users → First channel): bootstrap admin form is step 1 inside the wizard, collapsible connection blocks with inline Test, rail sub-items, Back / Skip for now / Continue nav, TV-static backdrop.
- Removed the webhook-handshake and Connect-Live-TV steps — Live TV auto-wires on a Tunarr save and now surfaces on that block's verdict.
- Replaced the `/channels` card grid with a Tunarr-style TV Guide (time ruler, channel rail, duration-scaled programs, filler pods, pending slots, live now-line, 2H/4H/6H zoom).
- Adopted the repo's channel vocabulary from `channel-health.ts` + the inline "Add a channel" suggest surface.
- Added a TV power-on entrance (SMPTE bars, wordmark, card) and staggered nav reveal.

## Screen map

| Screen (prototype) | Built from |
| --- | --- |
| Guide (`/channels`) | `web/apps/web/src/routes/_authed/channels/index.tsx`, `components/loomarr/channel-card/channel-card.tsx`, `components/loomarr/now-next-strip/now-next-strip.tsx`, `components/loomarr/on-air-indicator/on-air-indicator.tsx`, `channels/channel-health/channel-health.ts` |
| Palette, type, spacing, motion | `web/packages/tokens/generated/theme.css` |
| Channel detail | `web/apps/web/src/routes/_authed/channels/$id.tsx` (not yet fully reconciled) |
| Channel detail | `routes/_authed/channels/$id.tsx`, `-channel-programming.tsx`, `-channel-advanced.tsx`, `channel-nav`, `channel-policy-fields`, `channel-lineup-editor`, `channel-danger-zone`, `filler/channel-filler` |
| Setup wizard | `components/loomarr/wizard-shell/wizard-shell.tsx`, `wizard/steps/steps.ts`, `wizard/bootstrap-step`, `wizard/checklist-step`, `wizard/connect-step`, `wizard/tunarr-library-step`, `wizard/users-step`, `internal/api/setup.go` |
| Settings | `internal/settings/*`, `docs/config-design.md` (env > database > default) |

