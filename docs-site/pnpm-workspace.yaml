# pnpm refuses to run dependency build scripts unless they are named here. Mirrors the policy
# in web/pnpm-workspace.yaml: allow the ones that genuinely need to compile a native/platform
# binary, and let everything else stay blocked.
#
# esbuild ships per-platform binaries and cannot work without its postinstall.
onlyBuiltDependencies:
  - esbuild
