// Package main provides the explicit, non-CI planner tool-finalization
// diagnostic. It performs one bounded model call against fixed synthetic data
// and emits content-safe contract hashes and outcome metadata; it never logs
// prompt text, tool results, model output, credentials, or reasoning content.
package main
