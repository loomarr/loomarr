// Package main publishes a bounded, provider-free planner reference-host
// manifest from exact captured evidence. It never executes host commands,
// loads a model, starts inference, or contacts a provider.
package main
