> **Publication note:** `v0.1.0-beta.9` published GitHub notes, but its container image was never
> promoted because the release-source validator rejected obsolete CI job names. This release is
> its deployable successor and includes the full `v0.1.0-beta.9` feature set, including offline
> login for imported Emby/Jellyfin accounts, plus the validator correction.
